# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven - Week 2 hello world

on:
  push:
    paths:
    - 'myPracticeCourseWork/week_2/maven-test-exercise-hello_world/**'
  pull_request:
    paths:
    - 'myPracticeCourseWork/week_2/maven-test-exercise-hello_world/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build week_2 hello world project with Maven 
      run: mvn -B package --file pom.xml
      working-directory: myPracticeCourseWork/week_2/maven-test-exercise-hello_world